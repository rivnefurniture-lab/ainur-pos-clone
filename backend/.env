# Database Configuration - Railway PostgreSQL
# Do NOT add sslmode to URL - use poolConfig.ssl in database.ts
DATABASE_URL=postgresql://postgres:olegister14041992@autorack.proxy.rlwy.net:28902/railway

# Fallback for local (overridden by DATABASE_URL above)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ainur_pos
DB_USER=postgres
DB_PASSWORD=

# Server Configuration
PORT=3001
NODE_ENV=development

# Session Configuration
SESSION_SECRET=ainur-pos-clone-secret-key-change-in-production

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# JWT Secret (for token-based auth if needed)
JWT_SECRET=ainur-pos-jwt-secret-change-in-production
