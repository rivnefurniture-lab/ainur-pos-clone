# Database Configuration
# For production, use DATABASE_URL from your cloud database provider
DATABASE_URL=postgresql://user:password@host:5432/ainur_pos

# Or use individual settings for local development
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ainur_pos
DB_USER=
DB_PASSWORD=

# Server Configuration
PORT=3001
NODE_ENV=production

# Session Configuration (generate a secure secret for production)
SESSION_SECRET=your-secure-secret-key

# Frontend URL (for CORS)
FRONTEND_URL=https://your-frontend-url.vercel.app

# JWT Secret (for token-based auth if needed)
JWT_SECRET=your-jwt-secret
