# Database Configuration - REQUIRED for Railway/cloud
# Get from Railway dashboard: Project -> PostgreSQL -> Connect -> Connection URL
# Format: postgresql://postgres:PASSWORD@HOST:PORT/railway
DATABASE_URL=postgresql://postgres:olegister14041992@autorack.proxy.rlwy.net:28902/railway

# If DATABASE_URL fails (ECONNRESET): create new PostgreSQL on Railway, copy fresh DATABASE_URL

# Local fallback (ignored when DATABASE_URL is set)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ainur_pos
DB_USER=postgres
DB_PASSWORD=

# Server Configuration
PORT=3001
NODE_ENV=production

# Session Configuration (generate a secure secret for production)
SESSION_SECRET=your-secure-secret-key

# Frontend URL (for CORS)
FRONTEND_URL=https://your-frontend-url.vercel.app

# JWT Secret (for token-based auth if needed)
JWT_SECRET=your-jwt-secret
