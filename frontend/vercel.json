{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://ainur-pos-backend.railway.app/:path*" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
